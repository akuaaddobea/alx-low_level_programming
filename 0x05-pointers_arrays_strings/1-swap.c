#include "main.h"
#include <stdio.h>
/**
 * swap_int - swaps two integers' values
 * @a: first intger
 * @b: second integer
 * Return: returns nothing
 */
void swap_int(int *a, int *b);
{
	int c = *a; 

	*a = *b;
	*b = tmp;
}
